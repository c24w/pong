define(function () {

	require.config({

		paths: {
			'chai': '../node_modules/chai/chai'

			// not necessary while base path is src dir
			// because file names match aliases:
			// 'pong': 'pong',
			// 'options-parser': 'options-parser'
		}

	});

});